
        function foo(a: integer): integer;
        begin
            if d then
                return 1;
            else
                if d then
                        a:=1;
                    else
                        return 1;
            end

        procedure main();
        begin
        end

        var a: integer;
        var b: string;
        var c: real;
        var d: boolean;
        var e: array[1 .. 3] of integer;
        